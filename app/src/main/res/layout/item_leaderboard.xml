<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@android:color/transparent"
    android:layout_marginTop="25dp"
    android:id="@+id/itemLayout">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/personImageView"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintHorizontal_bias="0.1"
        tools:src="@drawable/squidward_profile_image"/>

    <TextView
        android:id="@+id/personName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/personImageView"
        app:layout_constraintLeft_toRightOf="@id/personImageView"
        app:layout_constraintRight_toLeftOf="@id/rank"
        android:layout_marginStart="30dp"
        android:paddingEnd="20dp"
        android:fontFamily="@font/sfuitext_regular"
        tools:text="Squidward Tentacles"
        android:textColor="#1f314a"
        android:textSize="18sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/personPoints"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/personName"
        app:layout_constraintLeft_toLeftOf="@id/personName"
        app:layout_constraintRight_toLeftOf="@id/rank"
        android:paddingEnd="20dp"
        android:fontFamily="@font/sfuitext_regular"
        tools:text="200,000 Points"
        android:textColor="#1f314a"
        android:textSize="15sp"
        android:alpha="0.6"/>
    
    <TextView
        android:id="@+id/rank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/personName"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/personPoints"
        android:layout_marginRight="30dp"
        android:background="@drawable/round_rank_text_background"
        android:fontFamily="@font/rubik"
        android:text="10"
        android:textSize="20dp"
        android:textColor="#22d0c4"
        android:gravity="center"
        android:alpha="0.2"/>

</androidx.constraintlayout.widget.ConstraintLayout>